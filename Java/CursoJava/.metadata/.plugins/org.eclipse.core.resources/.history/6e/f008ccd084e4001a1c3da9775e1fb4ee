package classeMetodo;

public class Construtor94 {
	/**
	 * Quando se chama o construtor e não se passa nenhum parâmetro
	 * esse é chamado de construtor padrão
	 * Classe herdada e construtor tem o mesmo nome
	 * A diferença entra método e construto e a ausência de retorno 
	 * pois o java já sabe que o retorno de um construtor é uma instância do tipo desejado
	 * 
	 * */
}
