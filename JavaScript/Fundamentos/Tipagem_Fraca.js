// Refere-se a quão dinâmica é a linguagem
// Em c se uma variável deve ser identificada(tipo, int, float, cha)
// Em JavaScript isso não acontece (Python também não)

let qualquer = 'Legal'

console.log(qualquer)
console.log(typeof(qualquer))

qualquer = 3.1415
console.log(qualquer)